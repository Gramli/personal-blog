import ClipLoader from "react-spinners/ClipLoader";
import Center from "./Center";

const Loading = () => {
    return (
        <Center>
            <ClipLoader size={100} />
        </Center>
    );
  };
  
  export default Loading;